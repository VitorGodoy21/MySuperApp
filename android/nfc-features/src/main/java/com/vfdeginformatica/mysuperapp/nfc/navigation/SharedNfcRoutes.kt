package com.vfdeginformatica.mysuperapp.nfc.navigation

/**
 * Rota compartilhada da tela de NFC, registrada tanto no `NavHost` do
 * `:app` quanto no do `:app-qrcode`.
 */
object SharedNfcRoutes {
    const val NFC_SCREEN = "nfc_screen"
}
